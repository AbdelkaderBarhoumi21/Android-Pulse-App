package com.example.pulse_app.core.utils

object AppConstants {
    const val DATABASE_NAME= "task_pulse.db"
    const val SYNC_WORK_NAME= "task_sync"
    const val DEVICE_PLATFORM = "android"
    const val NETWORK_TIMEOUT_SECONDS = 30L // Int is 32 bit and Long is 64 bit
}