package com.example.pulse_app.features.task.presentation.ui
/*
import androidx.compose.ui.test.*
import androidx.compose.ui.test.junit4.createComposeRule
import org.junit.Rule
import org.junit.Test

class TaskListScreenTest {
    @get:Rule val rule = createComposeRule()

    @Test fun shows_empty_state_text() {
        rule.setContent {
            TaskPulseTheme {
                // render the empty branch directly via a fake VM or the EmptyState component
                com.taskpulse.app.core.ui.components
                    .EmptyState(UiText.EMPTY_TASKS)
            }
        }
        rule.onNodeWithText(UiText.EMPTY_TASKS).assertIsDisplayed()
    }
}

 */
